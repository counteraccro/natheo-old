<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Administration - {% block title %} {% endblock %}</title>

    {% include 'include/cms-assets.html.twig' %}
    {% include 'admin/include/admin-assets.html.twig' %}
    {% block stylesheets %}{% endblock %}

</head>
<body>
    <div class="container-fluid">
        <div class="row flex-nowrap">

            {% include 'admin/include/left-menu.html.twig' %}

            <div class="col py-3 pt-1" id="content-admin" data-msg-leave="{{ "admin_system#Attention, si vous quittez cette page, les modifications non enregistrées seront perdues" | trans }}">

                {% include 'admin/include/top-menu.html.twig' %}
                {% block body %}{% endblock %}

                <div class="text-center p-3">
                    {{ app_name }} - {{ "" | dateFooter }} - {{ "admin_system#Version" | trans }} {{ app_version_str }}
                     | <a class="text-dark" href="{{ path('admin_dashboard_todo') }}"><i class="fa fa-hammer"></i> TODO</a>
                </div>

            </div>
        </div>
    </div>
    <div id="admin-block-modal">
        <!-- div pour les modal -->
    </div>
{% block javascripts %}{% endblock %}
</body>
</html>